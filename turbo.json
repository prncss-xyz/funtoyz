{
	"$schema": "https://turborepo.com/schema.json",
	"tasks": {
		"transit": {
			"dependsOn": ["^transit"]
		},
		"build": {
			"outputs": ["dist"],
			"dependsOn": ["^build"]
		},
		"check:tests": {
			"dependsOn": ["build"],
			"outputs": ["coverage"]
		},
		"//#check:knip": {},
		"check:types": {
			"dependsOn": ["transit", "build"]
		},
		"check": {
			"dependsOn": ["//#check:knip", "check:types", "check:tests"]
		},
		"dev": {
			"persistent": true,
			"cache": false
		}
	}
}
